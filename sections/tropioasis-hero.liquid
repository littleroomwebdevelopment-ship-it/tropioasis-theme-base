<section class="tropi-hero">
  <div class="hero-bg-circle"></div>

  <div class="hero-content">
    <div class="hero-copy">
      <div class="hero-pill">
        <span>💙</span>
        Salud tropical en cada sorbo
      </div>

      <h1>
        Nutre tu<br>
        cuerpo,<br>
        <span>eleva tu</span><br>
        <strong>energía</strong>
      </h1>

      <p>
        Jugos Cold Press, Wellness Shots e Infusiones hechos con ingredientes
        frescos, naturales y llenos de vida para tu bienestar diario.
      </p>

      <div class="hero-buttons">
        <a href="/collections/all" class="btn-primary">
          Comprar ahora
          <span>→</span>
        </a>

        <a href="/collections/functional-juices" class="btn-secondary">
          Descubrir más
        </a>
      </div>

      <div class="hero-benefits">
        <div>
          <span>🌱</span>
          <p>Ingredientes<br>Naturales</p>
        </div>

        <div>
          <span>💧</span>
          <p>Prensado<br>en Frío</p>
        </div>

        <div>
          <span>💗</span>
          <p>Bienestar<br>Integral</p>
        </div>

        <div>
          <span>🛡️</span>
          <p>Fortalece tu<br>Inmunidad</p>
        </div>
      </div>
    </div>

    <div class="hero-image-wrap">
      <div class="natural-badge">
        <strong>100%</strong>
        <span>Natural</span>
      </div>

      <img
        src="{{ 'hero-juices.png' | asset_url }}"
        alt="Jugos TropiOasis"
        class="hero-image"
      >
    </div>
  </div>

  <div class="hero-info-bar">
    <div>
      <span>🚚</span>
      <p>
        <strong>Entrega rápida</strong>
        <small>A todo Puerto Rico</small>
      </p>
    </div>

    <div>
      <span>✅</span>
      <p>
        <strong>Pagos seguros</strong>
        <small>100% protegidos</small>
      </p>
    </div>

    <div>
      <span>🌱</span>
      <p>
        <strong>Productos frescos</strong>
        <small>Hechos al momento</small>
      </p>
    </div>

    <div>
      <span>🎧</span>
      <p>
        <strong>Atención al cliente</strong>
        <small>¡Estamos para ti!</small>
      </p>
    </div>
  </div>
</section>

<style>
  .tropi-hero {
    position: relative;
    width: 100vw;
    min-height: calc(100vh - 105px);
    margin-left: calc(50% - 50vw);
    margin-right: calc(50% - 50vw);
    padding: 70px 7vw 120px;
    background:
      radial-gradient(circle at left top, #fff0bb 0 16%, transparent 16.5%),
      linear-gradient(115deg, #fffaf0 0%, #fffaf0 67%, #eefbf1 100%);
    overflow: hidden;
    box-sizing: border-box;
  }

  .hero-bg-circle {
    position: absolute;
    left: -90px;
    top: -60px;
    width: 310px;
    height: 310px;
    border-radius: 50%;
    background: rgba(255, 215, 72, .28);
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 1500px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: .85fr 1.15fr;
    align-items: center;
    gap: 40px;
  }

  .hero-pill {
    width: max-content;
    max-width: 100%;
    padding: 12px 28px;
    border: 2px solid #ec0057;
    border-radius: 999px;
    background: rgba(255,255,255,.55);
    color: #07112f;
    text-transform: uppercase;
    font-weight: 900;
    font-size: 14px;
    margin-bottom: 26px;
    box-sizing: border-box;
  }

  .hero-pill span {
    margin-right: 10px;
  }

  .hero-copy h1 {
    margin: 0 0 26px;
    font-family: 'Nunito', sans-serif;
    font-size: clamp(58px, 5.4vw, 98px);
    line-height: .92;
    letter-spacing: -3px;
    font-weight: 900;
    color: #00a7b7;
    text-shadow: 0 8px 0 rgba(0,167,183,.09);
  }

  .hero-copy h1 span {
    color: #ec0057;
  }

  .hero-copy h1 strong {
    color: #ff6f00;
    font-weight: 900;
  }

  .hero-copy p {
    max-width: 600px;
    margin: 0 0 34px;
    color: #07112f;
    font-size: 22px;
    line-height: 1.36;
    font-weight: 900;
  }

  .hero-buttons {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 40px;
    flex-wrap: wrap;
  }

  .hero-buttons a {
    height: 58px;
    padding: 0 36px;
    border-radius: 999px;
    text-decoration: none;
    text-transform: uppercase;
    font-weight: 900;
    font-size: 16px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
    transition: .22s ease;
  }

  .hero-buttons a:hover {
    transform: translateY(-2px);
  }

  .btn-primary {
    background: #ec0057;
    color: #fff;
    box-shadow: 0 18px 32px rgba(236,0,87,.26);
  }

  .btn-primary span {
    margin-left: 28px;
    font-size: 24px;
  }

  .btn-secondary {
    color: #00a7b7;
    border: 2px solid #00a7b7;
    background: rgba(255,255,255,.45);
  }

  .hero-benefits {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 26px;
    max-width: 620px;
  }

  .hero-benefits div {
    text-align: center;
    color: #07112f;
  }

  .hero-benefits span {
    display: block;
    font-size: 34px;
    margin-bottom: 8px;
  }

  .hero-benefits p {
    margin: 0;
    font-size: 14px;
    line-height: 1.05;
    font-weight: 900;
  }

  .hero-image-wrap {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    min-width: 0;
  }

  .hero-image {
    width: min(860px, 58vw);
    max-width: 100%;
    height: auto;
    display: block;
    filter: drop-shadow(0 30px 36px rgba(0,90,70,.12));
  }

  .natural-badge {
    position: absolute;
    right: 6%;
    top: 25%;
    z-index: 3;
    width: 118px;
    height: 118px;
    border-radius: 50%;
    background: #ec0057;
    color: #fff;
    display: grid;
    place-items: center;
    text-align: center;
    text-transform: uppercase;
    box-shadow: 0 16px 30px rgba(236,0,87,.24);
  }

  .natural-badge strong {
    font-size: 32px;
    line-height: .9;
  }

  .natural-badge span {
    display: block;
    font-size: 14px;
    font-weight: 900;
    margin-top: -24px;
  }

  .hero-info-bar {
    position: absolute;
    left: 50%;
    bottom: 34px;
    transform: translateX(-50%);
    z-index: 4;
    width: min(1120px, 78vw);
    min-height: 82px;
    background: rgba(255,255,255,.95);
    border-radius: 18px;
    box-shadow: 0 18px 42px rgba(0,0,0,.12);
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    overflow: hidden;
  }

  .hero-info-bar div {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 14px;
    padding: 0 22px;
    border-right: 1px solid rgba(0,0,0,.08);
    box-sizing: border-box;
  }

  .hero-info-bar div:last-child {
    border-right: 0;
  }

  .hero-info-bar span {
    font-size: 28px;
    flex-shrink: 0;
  }

  .hero-info-bar p {
    margin: 0;
    display: flex;
    flex-direction: column;
  }

  .hero-info-bar strong {
    color: #07112f;
    text-transform: uppercase;
    font-size: 14px;
    font-weight: 900;
  }

  .hero-info-bar small {
    color: #4d5360;
    font-size: 12px;
    font-weight: 800;
  }

  @media (max-width: 1200px) {
    .tropi-hero {
      padding: 55px 40px 130px;
    }

    .hero-copy h1 {
      font-size: clamp(54px, 6vw, 82px);
    }

    .hero-copy p {
      font-size: 20px;
    }

    .hero-info-bar {
      width: min(1050px, 88vw);
    }

    .hero-info-bar div {
      padding: 0 16px;
    }
  }

  @media (max-width: 1100px) {
    .tropi-hero {
      min-height: auto;
      padding: 50px 24px 80px;
    }

    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 34px;
    }

    .hero-pill {
      margin-inline: auto;
    }

    .hero-copy p {
      margin-inline: auto;
    }

    .hero-buttons {
      justify-content: center;
    }

    .hero-benefits {
      margin-inline: auto;
    }

    .hero-image {
      width: min(820px, 94vw);
    }

    .natural-badge {
      right: 13%;
      top: 12%;
    }

    .hero-info-bar {
      position: relative;
      left: auto;
      bottom: auto;
      transform: none;
      margin: 44px auto 0;
      width: min(900px, 94vw);
    }
  }

  @media (max-width: 850px) {
    .hero-info-bar {
      grid-template-columns: repeat(2, 1fr);
    }

    .hero-info-bar div {
      min-height: 78px;
    }

    .hero-info-bar div:nth-child(2) {
      border-right: 0;
    }

    .hero-info-bar div:nth-child(1),
    .hero-info-bar div:nth-child(2) {
      border-bottom: 1px solid rgba(0,0,0,.08);
    }
  }

  @media (max-width: 700px) {
    .tropi-hero {
      padding: 42px 18px 60px;
      background:
        radial-gradient(circle at left top, #fff0bb 0 20%, transparent 20.5%),
        linear-gradient(160deg, #fffaf0 0%, #fffaf0 58%, #eefbf1 100%);
    }

    .hero-bg-circle {
      width: 230px;
      height: 230px;
      left: -100px;
      top: -70px;
    }

    .hero-pill {
      padding: 10px 18px;
      font-size: 12px;
      margin-bottom: 22px;
    }

    .hero-copy h1 {
      font-size: clamp(44px, 13vw, 56px);
      line-height: .95;
      letter-spacing: -2px;
      margin-bottom: 22px;
    }

    .hero-copy p {
      font-size: 18px;
      line-height: 1.4;
      margin-bottom: 28px;
    }

    .hero-buttons {
      flex-direction: column;
      gap: 14px;
      margin-bottom: 34px;
    }

    .hero-buttons a {
      width: 100%;
      max-width: 360px;
      height: 54px;
      padding: 0 24px;
      font-size: 14px;
    }

    .btn-primary span {
      margin-left: 18px;
    }

    .hero-benefits {
      grid-template-columns: repeat(2, 1fr);
      gap: 22px 16px;
      max-width: 420px;
    }

    .hero-benefits span {
      font-size: 30px;
    }

    .hero-benefits p {
      font-size: 13px;
    }

    .hero-image {
      width: min(620px, 108vw);
    }

    .natural-badge {
      width: 90px;
      height: 90px;
      right: 2%;
      top: 12%;
    }

    .natural-badge strong {
      font-size: 24px;
    }

    .natural-badge span {
      font-size: 11px;
      margin-top: -18px;
    }

    .hero-info-bar {
      grid-template-columns: 1fr;
      width: 100%;
      margin-top: 32px;
      border-radius: 16px;
    }

    .hero-info-bar div {
      min-height: 74px;
      border-right: 0;
      border-bottom: 1px solid rgba(0,0,0,.08);
      justify-content: flex-start;
      padding: 0 22px;
    }

    .hero-info-bar div:last-child {
      border-bottom: 0;
    }

    .hero-info-bar span {
      font-size: 26px;
    }
  }

  @media (max-width: 480px) {
    .tropi-hero {
      padding: 36px 14px 50px;
    }

    .hero-pill {
      width: 100%;
      justify-content: center;
      text-align: center;
      display: flex;
      align-items: center;
      padding: 10px 14px;
      font-size: 11px;
    }

    .hero-copy h1 {
      font-size: clamp(40px, 14vw, 50px);
      letter-spacing: -1.6px;
    }

    .hero-copy p {
      font-size: 16px;
      max-width: 340px;
    }

    .hero-buttons a {
      max-width: 100%;
      height: 52px;
      font-size: 13px;
    }

    .hero-benefits {
      gap: 20px 12px;
    }

    .hero-image-wrap {
      margin-top: -6px;
    }

    .hero-image {
      width: 112vw;
      max-width: none;
      margin-left: -6vw;
    }

    .natural-badge {
      width: 76px;
      height: 76px;
      right: 0;
      top: 10%;
    }

    .natural-badge strong {
      font-size: 20px;
    }

    .natural-badge span {
      font-size: 10px;
      margin-top: -15px;
    }

    .hero-info-bar div {
      padding: 0 18px;
      gap: 12px;
    }

    .hero-info-bar strong {
      font-size: 13px;
    }

    .hero-info-bar small {
      font-size: 11px;
    }
  }

  @media (max-width: 380px) {
    .hero-copy h1 {
      font-size: 38px;
    }

    .hero-copy p {
      font-size: 15px;
    }

    .hero-benefits {
      grid-template-columns: 1fr 1fr;
    }

    .hero-benefits span {
      font-size: 28px;
    }

    .hero-benefits p {
      font-size: 12px;
    }

    .hero-info-bar div {
      min-height: 70px;
    }
  }
</style>

{% schema %}
{
  "name": "Tropi Oasis Hero",
  "settings": [],
  "presets": [
    {
      "name": "Tropi Oasis Hero"
    }
  ]
}
{% endschema %}